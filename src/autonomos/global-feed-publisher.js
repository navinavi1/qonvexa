import { businessSnapshot } from './business-snapshot.js';
import { isRetiredMarket } from './retired-markets.js';
import fs from 'node:fs';
import path from 'node:path';
import { publicDir } from './util.js';

export class GlobalFeedPublisher{
  constructor({env=process.env,storageDir='',logger=console}={}){this.env=env;this.logger=logger;this.root=path.join(storageDir||env.STORAGE_DIR||'data','autonomos');this.publicFile=path.join(publicDir(env),'autonomos-global-feed.json');this.timer=null;}
  start(){if(this.timer)return;const every=Math.max(5000,Number(this.env.AUTONOMOS_GLOBAL_FEED_PUBLISH_MS||10000));this.publish();this.timer=setInterval(()=>this.publish(),every);this.timer.unref?.();}
  stop(){if(this.timer)clearInterval(this.timer);this.timer=null;}
  publish(){try{
    const hunter=readJson(path.join(this.root,'global-work-hunter.json'),{}),actioner=readJson(path.join(this.root,'global-lead-actioner.json'),{}),taskforceWorker=readJson(path.join(this.root,'taskforce-worker.json'),{}),registry=readJson(path.join(this.root,'job-registry.json'),{}),moneyReport=readJson(path.join(this.root,'money-report.json'),{}),sourceScout=readJson(path.join(this.root,'free-market-scout.json'),{}),skillLibrary=readJson(path.join(this.root,'adaptive-skill-library.json'),{}),internetHunter=readJson(path.join(this.root,'internet-hunter.json'),{});
    const disabled=new Set(String(this.env.AUTONOMOS_DISABLED_MARKETS||'').split(',').map(x=>x.trim().toLowerCase()).filter(Boolean)),rows=[],seen=new Set();
    for(const lead of Object.values(hunter?.leads||{})){const source=String(lead?.source||'web').toLowerCase();if(disabled.has(source))continue;const id=String(lead?.id||lead?.url||'');if(!id||seen.has(id))continue;seen.add(id);const action=actioner?.actions?.[id]||{},savedStatus=String(action?.status||''),actionState=['applied','applied_email'].includes(savedStatus)&&!action.commentId&&!action.gmailMessageId?'application_uncertain':savedStatus,bucket=webActionBucket(actionState,lead);rows.push({id,title:String(lead?.title||'Paid digital work').slice(0,220),source,url:safeUrl(lead?.url),category:String(lead?.category||'general-digital'),amountUsd:num(action?.payout?.amountUsd??lead?.amountUsd),currency:String(action?.payout?.currency||lead?.payoutCurrency||'UNKNOWN').toUpperCase(),bucket,status:actionState||(lead?.applyReady?'ready':'new'),firstSeenAt:String(lead?.firstSeenAt||''),lastSeenAt:String(action?.updatedAt||lead?.lastSeenAt||''),appliedAt:String(action?.appliedAt||''),acceptedAt:String(action?.acceptedAt||''),submittedAt:String(action?.submittedAt||''),reason:String(action?.reason||action?.error||lead?.blocker||'').slice(0,220),cryptoPayout:Boolean(lead?.cryptoPayout),payoutVerified:Boolean(lead?.payoutVerified),attempts:Number(action?.attempts||0),applicationUrl:safeUrl(action?.applicationUrl),freeSource:String(lead?.freeSource||'')});}
    for(const [key,app] of Object.entries(hunter?.taskforce?.applications||{})){const task=hunter?.taskforce?.tasks?.[key]||{},worker=taskforceWorker?.tasks?.[key]||{},title=String(task?.title||app?.title||'TaskForce task');if(isTaskforceTest(title,task,app))continue;const appStatus=String(app?.status||'').toUpperCase(),workerStatus=String(worker?.status||''),bucket=taskforceBucket(appStatus,workerStatus);rows.push({id:`taskforce:${key}`,title:title.slice(0,220),source:'taskforce',url:safeUrl(task?.url),category:String(task?.category||app?.skill||worker?.skill||'digital'),amountUsd:num(task?.budgetUsd||app?.budgetUsd),currency:'USDC',bucket,status:workerStatus||appStatus.toLowerCase(),firstSeenAt:String(task?.observedAt||app?.appliedAt||''),lastSeenAt:String(worker?.updatedAt||app?.updatedAt||app?.appliedAt||task?.observedAt||''),appliedAt:String(app?.appliedAt||''),acceptedAt:String(worker?.acceptedAt||''),submittedAt:String(worker?.submittedAt||''),reason:String(worker?.submitError||worker?.qaReasons?.join?.('; ')||app?.error||'').slice(0,220),cryptoPayout:true,payoutVerified:true});}
    // The internet hunter probes agent-native markets on a timer and writes what it finds
    // to internet-hunter.json — which no other module reads, so an entire discovery lane was
    // invisible to the operator. Its rows are surfaced here like any other source.
    for(const [key,job] of Object.entries(internetHunter?.jobs||{})){
      const source=String(job?.source||'').toLowerCase();
      if(!source||disabled.has(source))continue;
      const id=`internet:${key}`;
      if(seen.has(id))continue;
      seen.add(id);
      const currency=String(job?.currency||'').toUpperCase();
      const status=String(job?.status||'open').toLowerCase();
      rows.push({id,title:String(job?.title||'Agent market task').slice(0,220),source,url:safeUrl(job?.url),category:String(job?.rawSummary?.category||'agent-market'),amountUsd:num(job?.amount),currency:currency||'UNKNOWN',bucket:job?.claimReady?'ready':'new',status,firstSeenAt:String(job?.observedAt||''),lastSeenAt:String(job?.observedAt||''),appliedAt:'',acceptedAt:'',submittedAt:'',reason:String(job?.reason||'').slice(0,220),cryptoPayout:['USDC','USDT','ETH','BTC','SOL','DAI'].includes(currency),payoutVerified:Boolean(job?.payoutVerified)});
    }
    for(const row of Object.values(registry||{})){const source=String(row?.source||'').toLowerCase();if(!source||disabled.has(source))continue;const title=String(row?.title||row?.externalId||'Marketplace job');if(source==='taskforce'&&isTaskforceTest(title,row,row))continue;const id=`registry:${row.identity||`${source}:${row.externalId||''}`}`;if(seen.has(id))continue;const status=String(row?.status||'new'),bucket=['dispatch_pending','bid_submitted','claimed','executing','qa'].includes(status)?'working':['delivered','completed'].includes(status)?'done':['paid','settled'].includes(status)?'paid':['archived','graveyard','stale_check','policy_hold','not_eligible','system_blocked','capability_hold','manual_attention','rejected','expired','cancelled'].includes(status)?'archive':status==='ready'?'ready':'new';rows.push({id,title:title.slice(0,220),source,url:safeUrl(row?.url),category:'marketplace',amountUsd:num(row?.budgetUsd),currency:String(row?.currency||'USD').toUpperCase(),bucket,status,firstSeenAt:String(row?.firstSeenAt||row?.createdAt||row?.lastSeenAt||''),lastSeenAt:String(row?.lastSeenAt||row?.lastStateAt||''),appliedAt:String(row?.bidSubmittedAt||row?.claimedAt||''),acceptedAt:String(row?.claimedAt||''),submittedAt:String(row?.deliveredAt||''),reason:String(row?.reason||row?.reasonCode||'').slice(0,220),cryptoPayout:['USDC','USDT','ETH','BTC','SOL','DAI'].includes(String(row?.currency||'').toUpperCase()),payoutVerified:false});}
    for(let i=rows.length-1;i>=0;i--)if(isRetiredMarket(rows[i])&&['new','ready'].includes(rows[i].bucket))rows[i].bucket='archive';
    rows.sort((a,b)=>time(b.firstSeenAt||b.appliedAt||b.lastSeenAt)-time(a.firstSeenAt||a.appliedAt||a.lastSeenAt)||String(a.id).localeCompare(String(b.id)));const counts={};for(const row of rows)counts[row.bucket]=(counts[row.bucket]||0)+1;const scoutCandidates=Object.values(sourceScout?.candidates||{}).filter(candidate=>!isRetiredMarket(candidate)).sort((a,b)=>Number(b?.score||0)-Number(a?.score||0)).slice(0,25).map(x=>({name:x.name,score:Number(x.score||0),status:x.status,repoUrl:safeUrl(x.repoUrl),homepage:safeUrl(x.homepage),payoutSignal:Boolean(x.payoutSignal),registrationSignal:Boolean(x.registrationSignal),humanGate:Boolean(x.humanGate)}));const business=businessSnapshot(this.env.STORAGE_DIR,this.env);const payload={business:{counts:business.counts,money:business.money,blockers:business.blockers,markets:business.markets.map(x=>({id:x.id,name:x.name,status:x.status,blocker:x.blocker,lastJobsCount:x.lastJobsCount})),workforce:{active:business.workforce.active}},generatedAt:new Date().toISOString(),scope:'worldwide',intervalSeconds:10,total:rows.length,counts,actioner:actioner?.stats||{},moneyReport,autonomy:{freeSources:hunter?.freeSources||{},marketScout:{lastScanAt:String(sourceScout?.lastScanAt||''),scans:Number(sourceScout?.scans||0),verifiedCandidates:scoutCandidates.filter(x=>x.status==='verified_candidate').length,candidates:scoutCandidates},skillLibrary:{generatedAt:String(skillLibrary?.generatedAt||''),topWorkflows:Array.isArray(skillLibrary?.topWorkflows)?skillLibrary.topWorkflows.slice(0,12):[]}},rows:rows.slice(0,1200)};fs.writeFileSync(this.publicFile,JSON.stringify(payload),{encoding:'utf8',mode:0o644});
  }catch(error){try{this.logger.warn?.('[GlobalFeedPublisher] '+String(error?.message||error).slice(0,180));}catch{}}}
}
function isTaskforceTest(title,task={},app={}){const t=String(title||'').toLowerCase();if(/\bbase chain probe\b|draft task for taskforce action testing|taskforce action testing|commissioning probe/.test(t))return true;return [task?.isTest,task?.test,task?.demo,app?.isTest,app?.test,app?.demo].some(Boolean);}
function webActionBucket(status,lead){if(['applied','applied_email'].includes(status))return'applied';if(['accepted','accepted_email','accepted_waiting_treasury','accepted_needs_capability','executing','executing_email','delivery_email_in_progress','submission_uncertain'].includes(status))return'working';if(['accepted_github','executing_github'].includes(status))return'working';if(['submitted','submitted_email'].includes(status))return'done';if(status==='paid')return'paid';if(['archived','human_gate','ai_prohibited','physical_or_employment','paid_registration_required','capability_blocked','accepted_repair_exhausted','no_direct_route'].includes(status))return'archive';if(['needs_capability','payout_unverified','registration_email_missing','inspect_or_apply_failed','direct_fetch_failed','direct_action_failed','email_channel_unavailable','email_rate_limited','application_uncertain','account_or_email_verification_required','native_api_route'].includes(status))return'new';return lead?.applyReady?'ready':'new';}
function taskforceBucket(appStatus,workerStatus){if(workerStatus==='paid')return'paid';if(workerStatus==='completed'||appStatus==='PAID_OR_APPROVED')return'done';if(['submitted'].includes(workerStatus)||appStatus==='SUBMITTED')return'done';if(['preparing','executing','submission_uncertain','waiting_agent_treasury'].includes(workerStatus)||['ACCEPTED','IN_PROGRESS','WORKING','SUBMISSION_REJECTED'].includes(appStatus))return'working';if(['rejected_after_repairs','blocked_capability','repair_exhausted','submit_failed'].includes(workerStatus)||['REJECTED','APPLY_FAILED'].includes(appStatus))return'archive';if(['PENDING','APPLIED'].includes(appStatus))return'applied';return'ready';}
function readJson(file,fallback){try{return JSON.parse(fs.readFileSync(file,'utf8'));}catch{return fallback}}
function safeUrl(value){const v=String(value||'');return /^https?:\/\//i.test(v)?v:''}
function num(value){const n=Number(value||0);return Number.isFinite(n)?n:0}
function time(value){const n=Date.parse(String(value||''));return Number.isFinite(n)?n:0}

