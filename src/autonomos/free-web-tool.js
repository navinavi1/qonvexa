import { reserveResource, observeResourceResult, resourceAvailability } from './resource-control.js';
const sleep=ms=>new Promise(r=>setTimeout(r,ms));
let lastSearchAt=0;
function cleanText(v=''){return String(v).replace(/<script[\s\S]*?<\/script>/gi,' ').replace(/<style[\s\S]*?<\/style>/gi,' ').replace(/<[^>]+>/g,' ').replace(/&nbsp;/gi,' ').replace(/&amp;/gi,'&').replace(/\s+/g,' ').trim();}
function decodeDuckUrl(href=''){try{const u=new URL(href,'https://html.duckduckgo.com');const raw=u.searchParams.get('uddg');return raw?decodeURIComponent(raw):u.href;}catch{return String(href||'');}}
async function freeWebSearchImpl(query,env=process.env,signal){const q=String(query||'').trim().slice(0,400);if(!q)return{ok:false,error:'empty_query'};const minGap=Math.max(1200,Number(env.AUTONOMOS_FREE_SEARCH_MIN_GAP_MS||2200));const wait=Math.max(0,lastSearchAt+minGap-Date.now());if(wait)await sleep(wait);lastSearchAt=Date.now();try{const r=await fetch('https://html.duckduckgo.com/html/?q='+encodeURIComponent(q),{headers:{'user-agent':'Mozilla/5.0 (compatible; AutonomOS/15.0; +https://qonvexa.co)'},signal:signal?AbortSignal.any([signal,AbortSignal.timeout(18000)]):AbortSignal.timeout(18000)});if(!r.ok)return{ok:false,error:'http_'+r.status,results:[]};const html=await r.text();const out=[];const re=/<a[^>]+class=["'][^"']*result__a[^"']*["'][^>]+href=["']([^"']+)["'][^>]*>([\s\S]*?)<\/a>/gi;let m;while((m=re.exec(html))&&out.length<8){const url=decodeDuckUrl(m[1]);if(!/^https?:\/\//i.test(url))continue;out.push({title:cleanText(m[2]).slice(0,220),url,snippet:''});}return{ok:true,provider:'free_public_web',results:out};}catch(e){return{ok:false,error:String(e?.message||e).slice(0,180),results:[]};}}
async function freeWebScrapeImpl(url,env=process.env,signal){const target=String(url||'').trim();if(!/^https?:\/\//i.test(target))return{ok:false,error:'invalid_url'};try{const r=await fetch(target,{headers:{'user-agent':'Mozilla/5.0 (compatible; AutonomOS/15.0; +https://qonvexa.co)'},redirect:'follow',signal:signal?AbortSignal.any([signal,AbortSignal.timeout(18000)]):AbortSignal.timeout(18000)});if(!r.ok)return{ok:false,error:'http_'+r.status};const body=await r.text();return{ok:true,url:r.url,content:cleanText(body).slice(0,12000)};}catch(e){return{ok:false,error:String(e?.message||e).slice(0,180)};}}

export async function freeWebSearch(query,env=process.env,signal){
  const allowance=await reserveResource('public_http',1,env);
  const duck=allowance.ok?await reserveResource('duckduckgo',1,env):allowance;
  const primary=duck.ok?observeResourceResult('duckduckgo',await freeWebSearchImpl(query,env,signal),env):duck;
  if(primary.ok&&primary.results?.length)return primary;
  const github=await reserveResource('github',1,env);if(!github.ok)return{...primary,replacementRequired:true,alternatives:['public_rss','wikipedia_public_api'],retryable:true};
  try{const u=new URL('https://api.github.com/search/repositories');u.searchParams.set('q',String(query||'').slice(0,300));u.searchParams.set('per_page','8');const r=await fetch(u,{headers:{accept:'application/vnd.github+json','user-agent':'AutonomOS-FreeFallback',...(env.GITHUB_TOKEN?{authorization:'Bearer '+env.GITHUB_TOKEN}:{})},signal:signal?AbortSignal.any([signal,AbortSignal.timeout(15000)]):AbortSignal.timeout(15000)});if(!r.ok)return observeResourceResult('github',{ok:false,error:'github_http_'+r.status,replacementRequired:true},env);const body=await r.json();return{ok:true,provider:'github_public_search',scope:'public_github_repositories',fallbackReason:primary.error||'primary_search_empty',results:(body.items||[]).map(x=>({title:x.full_name,url:x.html_url,snippet:x.description||''}))};}catch(error){return{ok:false,error:String(error.message),replacementRequired:true};}
}
export async function freeWebScrape(url,env=process.env,signal){const allowance=await reserveResource('public_http',1,env);if(!allowance.ok)return allowance;return freeWebScrapeImpl(url,env,signal);}
